To resolve the issue with LDAP group membership recognition in pfSense, you can follow these steps:

1. **Verify LDAP Configuration:**
   Ensure that the LDAP configuration in pfSense correctly matches the settings of your LDAP server. Double-check the Base DN, Bind DN, and password settings.

2. **Create a New User:**
   - In phpLDAPadmin, use the `inetOrgPerson` template to create a new user named `mahdi`. Be careful to avoid attributes that require a `gidNumber`, as your current configuration doesn't require it.
   - Enter the necessary details such as UID, CN, SN, and password. You can skip fields related to POSIX attributes if they are not relevant to your configuration.

3. **Add User to Group:**
   - Navigate to the `VPN-Users` group in phpLDAPadmin.
   - Edit the group entry and add the DN of the newly created user (`uid=mahdi,ou=people,dc=demo,dc=local`) to the `member` attribute list.

4. **Test Authentication in pfSense:**
   - Go to the pfSense dashboard and navigate to the Authentication Servers section.
   - Perform an authentication test with the credentials of the newly created user `mahdi`.
   - Check if pfSense recognizes the group membership of `VPN-Users`.

5. **Troubleshoot Common Issues:**
   - Make sure that there are no firewalls or network issues blocking secure LDAP (LDAPS) communication between pfSense and your LDAP server.
   - Verify the logs on both pfSense and your LDAP server for any errors related to LDAP queries or connection issues.
   - Check if pfSense has been configured to use the correct search filters and base DN for group membership queries.

6. **Consider Using LDIF for Direct LDAP Manipulation (Optional):**
   - If you prefer not to use phpLDAPadmin, you can manipulate LDAP entries directly using LDIF files.
   - Use a text editor to create an LDIF file for adding a user and their group membership, then use `ldapadd` commands to apply changes to your LDAP directory.

7. **Enable RFC 2307 Compatibility if Required:**
   - Check if enabling the RFC 2307 Groups option in pfSense makes a difference. This may assist with identifying groups correctly if your LDAP setup uses them for POSIX compatibility.

By carefully following these steps and verifying each part of the process, you should be able to resolve the group membership recognition issue in pfSense. If problems persist, consider consulting LDAP and pfSense forums for additional community support.