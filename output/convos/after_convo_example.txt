**Context Bootstrap:**

The user is integrating LDAP with a pfSense router for central user management but is facing issues with group membership queries not being recognized. The current LDAP configuration is set to Directory-style with groups defined as `groupOfNames` and members listed as DNs. The user has verified that the LDAP server is correctly configured, but pfSense is not displaying group memberships during authentication tests.

**Technical Environment:**
- LDAP Server: `ldaps://ldap.demo.local`
- Base DN: `dc=demo,dc=local`
- User DN: `uid=mahdi,ou=people,dc=demo,dc=local`
- Group DN: `cn=VPN-Users,ou=groups,dc=demo,dc=local`
- Group Object Class: `groupOfNames`
- Group Member Attribute: `member`
- User Object Class: `inetOrgPerson` (to be used for new users)

**What Has Been Tried:**
- LDAP queries confirmed that `testuser` is correctly in the `VPN-Users` group.
- The user `mahdi` was not found in LDAP, and attempts to create it using the wrong template (posixAccount) resulted in errors.
- The user was advised to create a new user with the correct object class and add it to the group.

**Next Steps:**
1. Create the group `VPN-Users` in phpLDAPadmin if it doesn't exist.
2. Create a new user `mahdi` using the `inetOrgPerson` template, ensuring no `gidNumber` is required.
3. Add `mahdi` to the `VPN-Users` group by modifying the group's member attribute.
4. Test the authentication in pfSense to verify that the group membership is recognized.

The user is encouraged to follow the outlined steps in the phpLDAPadmin GUI to resolve the issue without needing command-line interactions.