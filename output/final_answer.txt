Based on the logs and information provided, there are a few potential reasons for the unresponsiveness of the endpoint:

1. **CORS Error**: 
   - The CORS error in `root-backend-1` indicates that a request to this service's endpoint is being blocked due to a Cross-Origin Resource Sharing issue. This typically means that the frontend or another service trying to access this endpoint isn't allowed by the backend's CORS policy.
   - Since CORS errors occur at the preflight or initial stages of a request, it can result in the endpoint appearing unresponsive to the client making the request. You will need to check the CORS configuration of `root-backend-1` and ensure that the client origin is allowed.
   
2. **Database Schema Issue**: 
   - The error in your PostgreSQL logs indicates that there is an issue recognizing the column `cv_submissions.revision`. This could suggest that the database schema is not in sync with the application's expectations.
   - If the application endpoint relies on querying or updating the `cv_submissions` table and expects the `revision` column, the absence of this column could cause operations to fail, leading to an unresponsive endpoint.
   - To resolve this, you should verify that the database migrations have been correctly applied, and the schema matches the application's requirements.

### Steps to Resolve:
1. **Verify CORS Settings**:
   - Update the `root-backend-1`'s CORS settings to ensure that the frontend's origin is included in the allowed origins. You might need to modify the server configuration or middleware responsible for handling CORS.

2. **Database Migration**:
   - Review your database migration history and ensure all necessary migrations have been applied correctly. You may need to run pending migrations or update your migration scripts to correctly modify the `cv_submissions` table structure, ensuring that the `revision` column is correctly created.

3. **Testing and Monitoring**:
   - After addressing these issues, test the endpoint again to check if it becomes responsive.
   - Monitor application logs for any further errors or issues that could provide additional insights.

By addressing these potential issues, you should be able to resolve the unresponsiveness of the endpoint.