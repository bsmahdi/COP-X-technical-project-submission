The production server's endpoint is unresponsive. Check the logs for the `root-backend-1` container for CORS errors and the `cv_submissions` table for missing `revision` column errors. The service is running on `http://0.0.0.0:5005`.